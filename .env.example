# Sao chép thành .env (không commit .env).
# Mật khẩu SA: đủ mạnh theo Microsoft (độ dài, hoa, thường, số, ký tự đặc biệt).

MSSQL_SA_PASSWORD=QlscDocker2026!

# Tuỳ chọn: nếu cổng 80 đã bị chiếm (IIS, Apache, …)
# NGINX_HTTP_PORT=8088

# Tuỳ chọn: cổng SQL trên máy host (mặc định 14333, tránh trùng SQL cài sẵn ở 1433)
# SQLSERVER_HOST_PORT=14333
